你是 LaTeX 排版专家。以下 LaTeX 文档编译出错，请修复所有错误并返回完整的修正后 LaTeX 源码。

## 编译错误信息
```
{{ errors }}
```

## 当前 LaTeX 源码
```latex
{{ latex_content }}
```

## 要求
1. 只修复编译错误，不要改变文档的内容和结构
2. 常见错误修复：缺少宏包、命令拼写错误、括号不匹配、环境未关闭等
3. 确保输出使用 UTF-8 编码，支持中文（ctex 宏包）
4. 直接输出完整的修正后 LaTeX 源码，不要添加任何解释
5. 不要用 ```latex ``` 包裹，直接输出 LaTeX 代码，以 \documentclass 开头
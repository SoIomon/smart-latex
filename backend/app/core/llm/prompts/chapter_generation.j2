你是专业的 LaTeX 排版专家。请根据大纲和源文档内容，生成该章节的 LaTeX 代码。

## 文档整体信息
- 标题：{{ doc_title }}
- 当前章节：第 {{ chapter_index }} 章 / 共 {{ total_chapters }} 章
- 章节标题：{{ chapter.title }}
- 章节描述：{{ chapter.description }}

{% if chapter.subsections %}
## 子节结构
{% for sub in chapter.subsections %}
- {{ sub.title }}：{{ sub.description }}
{% endfor %}
{% endif %}

## 源文档内容
以下是该章节对应的源文档分析和原文，请尽可能完整地保留源文档中的关键数据、技术参数、结论等实质性内容，不要概括或省略重要细节：

注意：原文中 [IMAGE: ...] 标记表示源文档中的图片及其内容描述，请按照 LaTeX 代码规范第 13 条处理。

{% for doc in source_documents %}
### 源文档 {{ loop.index }}：{{ doc.filename }}
**分析摘要：** {{ doc.analysis.abstract }}

**相关章节原文：**
{% for sec in doc.analysis.sections %}
#### {{ sec.heading }}
{{ sec.summary }}
要点：{{ sec.key_points | join('；') }}
{% endfor %}

**原文内容：**
{{ doc.content[:15000] }}
{% endfor %}

{% if template_rules %}
## 排版格式要求（必须严格遵守）
{{ template_rules }}
{% endif %}

## LaTeX 代码规范（必须严格遵守）
1. 使用 {{ section_commands.top }}{ {{ chapter.title }} } 作为本章的顶级标题
2. 子节使用 {{ section_commands.second }}{}，更深层级依次用 {{ section_commands.third }}{}、{{ section_commands.fourth }}{}
3. 如果内容中有数据对比，尽量使用 \begin{table} ... \end{table} 呈现
4. 如果有列表内容，使用 \begin{itemize} 或 \begin{enumerate}
5. 重要术语可用 \textbf{} 加粗
6. 确保中文内容正确，不要乱码
7. 不要输出 \documentclass、\usepackage、\begin{document} 等文档级别命令，也不要输出 \setCJKmainfont、\setCJKsansfont、\setCJKmonofont、\newCJKfontfamily 等字体定义命令。字体和宏包已在模板导言区配置完毕，只输出章节内容
8. 只输出 LaTeX 代码，不要包含解释说明
9. 不要用 ```latex``` 包裹
10. 数学符号必须用 LaTeX 命令：≥ 写成 $\geq$，≤ 写成 $\leq$，± 写成 $\pm$，° 写成 $^\circ$，希腊字母用 $\alpha$、$\beta$ 等
11. 尽量保留源文档中的具体数据、参数、公式，不要用"等"或"..."省略
12. 如需切换字体，使用 \songti、\heiti、\kaishu、\fangsong 等 ctex 预定义的字体切换命令即可
13. 源文档中的 [IMAGE: 文件名, 描述: 图片描述, width=宽度cm] 占位符表示原文档中的图片。处理规则：
   a) figure 环境结构（严格按此格式，不得在 figure 内添加任何额外文字）：
      \begin{figure}[htbp]
        \centering
        \includegraphics[width=宽度]{images/文件名}
        \caption{简短标题}
        \label{fig:ch{{ chapter_index }}_文件名去掉扩展名}
      \end{figure}
   b) caption 必须简短精炼，不超过 15 个中文字符（如"激光通信终端外观"、"系统架构示意图"）。绝对不要把图片的详细描述放进 caption。
   c) 图片的详细说明、注释、数据来源等内容，写在 figure 环境之后的正文段落中，如"如图\ref{fig:ch{{ chapter_index }}_xxx}所示，该设备采用了..."
   d) figure 环境内部只允许 \centering、\includegraphics、\caption、\label 四个命令，禁止放入 \textbf、\small、注释文字、来源说明等任何其他内容。
   e) 图片路径必须加 images/ 前缀。宽度超过 15cm 时改用 width=0.9\textwidth。
   f) label 命名规则：fig:ch{{ chapter_index }}_文件名去掉扩展名（加章节前缀防止跨章节重复）。
14. 引用图片时，必须使用 \ref{fig:ch{{ chapter_index }}_label} 交叉引用，禁止硬编码"图1""图2"等编号。引用格式示例："如图\ref{fig:ch{{ chapter_index }}_xxx}所示"。只引用本章内定义的图片 label，不要引用其他章节的图片。
15. 表格处理规则（与图片同等重要，必须严格遵守）：
   a) table 环境结构：
      \begin{table}[htbp]
        \centering
        \caption{简短标题}
        \label{tab:ch{{ chapter_index }}_描述性名称}
        \begin{tabular}{...} ... \end{tabular}
      \end{table}
   b) caption 简短精炼，不超过 15 个中文字符。
   c) label 命名规则：tab:ch{{ chapter_index }}_描述性名称（加章节前缀防止跨章节重复）。
16. 引用表格时，必须使用 \ref{tab:ch{{ chapter_index }}_label} 交叉引用，禁止硬编码"表1""表3.3"等编号。引用格式示例："如表\ref{tab:ch{{ chapter_index }}_xxx}所示"。只引用本章内定义的表格 label，不要引用其他章节的表格。
17. 所有交叉引用一律使用 \ref{} 命令，绝对禁止在正文中硬编码任何编号（如"图1""表3.3""公式2"等）。这包括图片、表格、公式、章节等所有可编号对象。

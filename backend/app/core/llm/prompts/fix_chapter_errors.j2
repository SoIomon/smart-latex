你是 LaTeX 排版专家。以下 LaTeX 章节片段在编译时出错，请修复语法错误并返回修正后的章节内容。

## 编译错误信息
```
{{ errors }}
```

## 章节 LaTeX 片段
```latex
{{ chapter_content }}
```

## 要求
1. 只修复编译语法错误，不要改变文档的内容和结构
2. 常见错误修复：命令拼写错误、括号不匹配、环境未关闭、特殊字符未转义等
3. 这是一个章节片段（不是完整文档），不要添加 \documentclass、\begin{document} 等
4. 直接输出修正后的章节 LaTeX 代码，不要添加任何解释
5. 不要用 ```latex ``` 包裹
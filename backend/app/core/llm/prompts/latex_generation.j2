你是一个专业的LaTeX文档生成助手。请根据以下结构化内容和LaTeX模板，生成完整的LaTeX文档。

结构化内容：
```json
{{ structured_content }}
```

LaTeX模板（使用Jinja2自定义分隔符 << >> 表示变量，<% %> 表示控制块）：
```latex
{{ template_content }}
```

要求：
1. 将结构化内容填入模板，生成完整可编译的LaTeX文档
2. 确保所有LaTeX特殊字符正确转义
3. 确保中文内容使用ctex包支持
4. 确保文档结构完整，可以直接用xelatex编译
5. 只输出LaTeX代码，不要包含其他说明文字

请直接输出完整的LaTeX文档代码：
```latex
（在这里输出LaTeX代码）
```

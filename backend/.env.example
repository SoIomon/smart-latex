# 豆包（火山引擎）API 配置
# 首次安装后可通过 Web UI「设置」页面修改以下配置
DOUBAO_API_KEY=your-api-key-here
# 支持任何 OpenAI 兼容 API，按需修改 BASE_URL（如自建代理地址）
DOUBAO_BASE_URL=https://ark.cn-beijing.volces.com/api/v3
DOUBAO_MODEL=your-endpoint-id

# 数据库
DATABASE_URL=sqlite+aiosqlite:///./storage/smart_latex.db

# 存储目录
STORAGE_DIR=./storage

# LaTeX 编译器路径（macOS 默认 /Library/TeX/texbin/latexmk，Linux 默认 latexmk）
LATEX_CMD=latexmk

# CORS 允许的来源
CORS_ORIGINS=["http://localhost:5173","http://localhost:8000"]

# 日志配置
LOG_LEVEL=DEBUG
LOG_FILE=logs/smart_latex.log
